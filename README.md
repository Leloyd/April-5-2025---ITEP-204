# April-5-2025---ITEP-204

Use SQL Promtp

CREATE DATABASE crud_db;

USE crud_db;

CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    score INT
);
